'use strict';
var assert = require('assert');
var cartodbLayers = require('../');

describe('cartodb-layers authentication', function () {
  it('must have successfully authenticated the user', function () {
    assert(true);
  });
});
